#!/bin/bash
# Custom AppRun script for NipaPlay AppImage

SELF=$(readlink -f "$0")
HERE=${SELF%/*}

# Set library path
export LD_LIBRARY_PATH="${HERE}/lib:${LD_LIBRARY_PATH}"

# Execute the binary directly from AppImage root
exec "${HERE}/NipaPlay" "$@"
